# `colab_file_converter.ipynb` — File conversion helpers for Colab

Overview
- Utility notebook that contains cells for converting between file formats commonly used when preparing datasets in Colab: text cleaning, format conversion, and simple file operations.

Key features
- Convert DjVu/TXT outputs into normalized `.txt` files.
- Helpers to detect file encodings and re-save as UTF-8.
- Small utilities to move or rename files in bulk using safe patterns.

Run notes
- Likely contains Colab-specific commands (e.g., `!pip install` or `drive.mount`). Check the first few cells and adjust paths before running.

- **Last updated**: 2025-11-14
